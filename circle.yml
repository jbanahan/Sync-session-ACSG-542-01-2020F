machine:
  timezone: UTC
  ruby:
    version: ruby-2.1.6
dependencies:
  pre:
    - mkdir ./tmp
    - mkdir ./spec/support/tmp
test:
  post:
    - bundle exec rake spec:javascript:
        environment:
          RAILS_ENV: test