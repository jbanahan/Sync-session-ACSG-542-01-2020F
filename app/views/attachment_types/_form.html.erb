<%= form_for(@attachment_type) do |f| %>
  <% if @attachment_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attachment_type.errors.count, "error") %> prohibited this attachment_type from being saved:</h2>

      <ul>
      <% @attachment_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :module_type %><br />
    <%= f.text_field :module_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
