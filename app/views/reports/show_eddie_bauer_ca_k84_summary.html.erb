<% content_for :action_bar do %>
  <button class="form_to" form_id="frm_run">Run Report</button>
  <button class="btn_link" link_to="<%=request.referrer%>">Back</button>
<%end%>

<h2>Eddie Bauer CA K84 Summary</h2>
<p>Select the K84 due date and click the Run Report button to continue.</p>

<form class="form-horizontal" action="<%= reports_run_eddie_bauer_ca_k84_summary_path %>" method="POST" role="form" id="frm_run">
  <input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
  <div class="col-md-6">
    <div class="form-group">
      <div class="col-md-3"><label class="control-label" for="date">Due date:</label></div>
      <div class="col-md-3"><%=text_field_tag :date,"", :class=>'form-control isdate'%></div>
    </div>
  </div> 
</form>
